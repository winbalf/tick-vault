# tick-vault
A streaming data pipeline that ingests real-time crypto trade ticks, stores raw events in a GCS bronze layer, and spread metrics through a dbt silver/gold medallion architecture on BigQuery — visualised in Grafana.
